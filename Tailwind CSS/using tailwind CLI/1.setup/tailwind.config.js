/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["*.html"],
  theme: {
    extend: {},
  },
  plugins: [],
}

// content: ["index.html"],
// content: ["*"], <-- it increase load time because it includes all utility class
